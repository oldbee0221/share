<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seoul.share">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".util.TutorialActivity" />
        <activity android:name=".util.Rental_billigeActivity" />
        <activity android:name=".util.Rental_itemActivity" />
        <activity android:name=".util.MypagemainActivity" />
        <activity android:name=".util.RentalMainActivity" />
        <activity android:name=".util.SearchmainActivity" />
        <activity android:name=".util.Completed_pwActivity" />
        <activity android:name=".util.FInd_pwActivity" />
        <activity android:name=".deprecated.LoginActivity" />
        <activity android:name=".util.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".util.MainActivity" />
    </application>

</manifest>